{
  "name": "3snet-eventswidget-playwright",
  "version": "1.0.0",
  "private": true,
  "description": "Автоматизированные тесты Playwright для конструктора календаря мероприятий 3S.INFO",
  "scripts": {
    "pretest": "playwright install",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0"
  },
  "engines": {
    "node": ">=18"
  }
}
